package recursionpractice;

import java.util.Scanner;

/**
 * Ask the user for an amount of cents, and print minimum number of coins needed to equate the amount of cents
 * 
 * Note: This should be a recursive function(you can change the parameters if you would like and in fact I recommend
 * that you do)
 *
 */

public class Coins {
	public static void main(String[] args) {
		Scanner readInput = new Scanner(System.in);
		System.out.println("Enter cents: ");
		
		System.out.println(minimumCoins(readInput.nextInt()));
	}
	
	private static Integer minimumCoins(Integer c) {
		/*Write your recursive statement here*/
	}
	
	//test your code by not only running it, but tracing it below
	
	/*
	 * Trace here:
	 * 
	 */
}
